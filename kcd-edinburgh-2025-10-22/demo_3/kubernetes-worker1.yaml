version: 1.1.0
variant: flatcar
storage:
  links:
    - target: /opt/extensions/kubernetes/kubernetes-v1.33.0-x86-64.raw
      path: /etc/extensions/kubernetes.raw
      hard: false
  files:
    - path: /opt/extensions/kubernetes/kubernetes-v1.33.0-x86-64.raw
      contents:
        source: http://10.0.2.2:8000/kubernetes-v1.33.0-x86-64.raw
    - path: /etc/sysupdate.kubernetes.d/kubernetes-v1.33.conf
      contents:
        source: http://10.0.2.2:8000/kubernetes-v1.33.conf
    - path: /etc/sysupdate.d/noop.conf
      contents:
        source: http://10.0.2.2:8000/noop.conf
    - path: /etc/systemd/network/00-vmbus.network
      contents:
        inline: |
          [Match]
          Name=eth1
          [Network]
          Address=192.168.254.2/24
    - path: /etc/hostname
      contents:
        inline: |
          worker1

    - path: /etc/ssl/certs/zot.pem
      contents:
        local: zot/cert/zot.crt
    - path: /etc/containerd/config.toml
      contents:
        local: containerd/config.toml

systemd:
  units:
    - name: systemd-sysupdate.timer
      enabled: true
      dropins:
        - name: bootcheck.conf
          contents: |
            [Timer]
            OnBootSec=1min
            OnUnitActiveSec=10s
            RandomizedDelaySec=1s
    - name: systemd-sysupdate.service
      dropins:
        - name: kubernetes.conf
          contents: |
            [Service]
            ExecStartPre=/usr/bin/sh -c "readlink --canonicalize /etc/extensions/kubernetes.raw > /tmp/kubernetes"
            ExecStartPre=/usr/lib/systemd/systemd-sysupdate -C kubernetes update
            ExecStartPost=/usr/bin/sh -c "readlink --canonicalize /etc/extensions/kubernetes.raw > /tmp/kubernetes-new"
            ExecStartPost=/usr/bin/sh -c "if ! cmp --silent /tmp/kubernetes /tmp/kubernetes-new; then touch /run/reboot-required; fi"

    - name: kubeadm.service
      enabled: true
      contents: |
        [Unit]
        Description=Kubeadm service
        Requires=containerd.service
        After=containerd.service
        ConditionPathExists=!/etc/kubernetes/kubelet.conf
        [Service]
        Type=oneshot
        ExecStart=/usr/bin/kubeadm join 192.168.254.1:6443 -v10 --token "${KUBEADM_TOKEN}" --discovery-token-unsafe-skip-ca-verification
        [Install]
        WantedBy=multi-user.target
      dropins:
        - name: token.conf
          contents_local: token.conf
    - name: kubelet.service
      dropins:
        - name: 00-node-ip.conf
          contents: |
            [Service]
            Environment="KUBELET_EXTRA_ARGS=--node-ip 192.168.254.2"
    - name: locksmithd.service
      mask: true

    - name: mnt.mount
      enabled: true
      contents_local: mnt.mount
    - name: containerd.service
      dropins:
        - name: config.conf
          contents_local: containerd/config.conf
